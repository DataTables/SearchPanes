<?xml version="1.0" encoding="UTF-8" ?>
<dt-option library="SearchPanes">
	<name>searchPanes.hideCount</name>
	<summary>Hide the count column in all panes</summary>
	<since>SearchPanes 1.0.0</since>

	<type type="boolean">
		<description>
            By setting the `-init searchPanes.hideCount` option to `true` the count column will be hidden in all of the panes.
        </description>
	</type>

	<default value="false">
        The default value for the `-init searchPanes.hideCount` paramater is `false`, meaning that as standard the count column will be included in the panes.
	<description>
		<![CDATA[
        As standard, SearchPanes will be displayed with the count column included in the table. However if the value of `-init searchPanes.hideCount` is set to 'true' then the count column will no longer be displayed in any of the panes.

        The count column can be hidden for individual panes by the `-init columns.searchPanes.hideCount` option.
        ]]>
	</description>

	<example title="Hide count for all panes"><![CDATA[

$(document).ready(function() {
    $('#example').DataTable( {
		dom: 'Sfrtip',
        searchPanes:{
            hideCount: true
        }
    });
});

]]></example>

<related>-init columns.searchPanes.hideCount</related>

</dt-option>
