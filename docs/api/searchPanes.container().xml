<?xml version="1.0" encoding="UTF-8" ?>
<dt-api library="SearchPanes">
	<name>searchPanes.container()</name>
	<summary>Returns the node of the panes container.</summary>
	<since>1.0.0</since>

	<type type="function">
		<signature>searchPanes.container()</signature>
		<description>Returns the node of the panes container.</description>
		<returns type="jQuery">
            jQuery instance that contains the container element for the searchPanes.
        </returns>

	</type>

	<description>
		This method provides the ability to return the node of the container that is storing all of the SearchPanes.

        As it is a getter it does not take any arguments

	</description>

	<example title="Add a class to the searchPanes container:"><![CDATA[
    var dt = $('#example').DataTable( {
        dom: 'Pfrtip'
    });
	dt.searchPanes.container().addClass('sp_class');
]]></example>

</dt-api>
