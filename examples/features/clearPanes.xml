<?xml version="1.0" encoding="UTF-8" ?>
<dt-example table-type="html" table-class="display nowrap" order="10">

<css lib="datatables searchpanes select"/>

<js lib="jquery datatables searchpanes select">
<![CDATA[

$(document).ready(function() {
    $('#example').DataTable( {
        searchPanes:{
            clear:false,
        },
        dom: 'Pfrtip'
    });
});

]]>
</js>

<title lib="searchPanes">Disable Clear Buttons for Panes</title>

<info><![CDATA[

This is the searchPanes extension for DataTables. It allows results to be filtered based on the values of columns.

This shows how to stop the clear buttons being displayed by setting the `-init searchPanes.clear` config option to be `false`. 

`-init searchPanes.clear` is `true` by default.
]]></info>

</dt-example>
