<?xml version="1.0" encoding="UTF-8" ?>
<dt-example table-type="html" table-class="display nowrap" order="6">

<css lib="datatables searchpanes select"/>

<js lib="jquery datatables searchpanes select">
<![CDATA[

$(document).ready(function() {
    $('#example').DataTable({
        searchPanes: {
            controls: false
        },
        dom: 'Plfrtip'
    });
});

]]>
</js>

<title lib="SearchPanes">Hide all controls on all panes</title>

<info><![CDATA[

This is the searchPanes extension for DataTables. It allows results to be filtered based on the values of columns.

This example shows how to hide the Controls and disable searching for all of the panes can be hidden by setting the `-init searchPanes.controls` config option to `false`.

]]></info>

</dt-example>
