<?xml version="1.0" encoding="UTF-8" ?>
<dt-example table-type="html-wide" table-class="display nowrap" order="2">

<css lib="datatables searchpanes select"/>

<js lib="jquery datatables searchpanes select">
<![CDATA[

$(document).ready(function() {
    $('#example').DataTable( {
        searchPanes:{
            countWidth: '100px',
        }
    });
});

]]>
</js>

<title lib="SearchPanes">Width of Count Column</title>

<info><![CDATA[

This is the searchPanes extension for DataTables. It allows results to be filtered based on the values of columns.

This example shows how to customise the width of the count column by making use of the `countWidth` config option.

The default for `countWidth` is 50px.

]]></info>

</dt-example>