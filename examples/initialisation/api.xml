<?xml version="1.0" encoding="UTF-8" ?>
<dt-example table-type="html" table-class="display nowrap" order="2">

<css lib="datatables searchpanes select"/>

<js lib="jquery datatables searchpanes select">
<![CDATA[

var table = $('#example').DataTable();
new DataTable.SearchPanes(table, {});

table.searchPanes.container().prependTo(table.table().container());
table.searchPanes.resizePanes();

]]>
</js>

<js-vanilla>
<![CDATA[

let table = new DataTable('#example');
new DataTable.SearchPanes(table, {});

table.searchPanes.container().prependTo(table.table().container());
table.searchPanes.resizePanes();

]]>
</js-vanilla>

<title lib="SearchPanes">API Initialisation</title>

<info><![CDATA[


This example shows how to initialise SearchPanes through the `DataTable.SearchPanes` constructor. Note that it is unlikely you'll ever need to do this, but it is included here for completeness.

]]></info>

</dt-example>