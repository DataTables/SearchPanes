<?xml version="1.0" encoding="UTF-8" ?>
<dt-example table-type="html" table-class="display nowrap" order="10">

<css lib="datatables searchpanes select"/>

<js lib="jquery datatables searchpanes select">
<![CDATA[

$('#example').DataTable({
	layout: {
		top1: {
			searchPanes: {
				cascadePanes: true,
				viewTotal: true
			}
		}
	}
});

]]>
</js>

<js-vanilla>
<![CDATA[

new DataTable('#example', {
	layout: {
		top1: {
			searchPanes: {
				cascadePanes: true,
				viewTotal: true
			}
		}
	}
});

]]>
</js-vanilla>

<title lib="SearchPanes">Viewing Totals, Subtotals and Cascading Panes</title>

<info><![CDATA[

This example demonstrates the `-feature searchPanes.cascadePanes` feature working alongside the `-feature searchPanes.viewTotal` feature. Cascading panes allow the panes to be filtered based on the values selected in the other panes.

Both `-feature searchPanes.cascadePanes` and `-feature searchPanes.viewTotal` are `false` by default.

]]></info>

</dt-example>
