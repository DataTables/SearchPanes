<?xml version="1.0" encoding="UTF-8" ?>
<dt-example table-type="html" table-class="display nowrap" order="1">

<css lib="datatables searchpanes select"/>

<js lib="jquery datatables searchpanes select">
<![CDATA[

$(document).ready(function() {
    $('#example').DataTable({
        dom: 'Plfrtip',
        searchPanes: {
            initCollapsed: true
        }
    });
});

]]>
</js>

<title lib="searchPanes">Collapsed Panes</title>

<info><![CDATA[

This example shows how the `-init searchPanes.initCollapsed` option can be used to collapse all of the panes on initialisation. To do this the `-init searchPanes.collapse` option must also be set to true, which is the default.

]]></info>

</dt-example>
