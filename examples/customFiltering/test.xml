<?xml version="1.0" encoding="UTF-8" ?>
<dt-example table-type="html" table-class="display nowrap" order="7">

<css lib="datatables searchpanes select colreorder"/>

<js lib="jquery datatables searchpanes select colreorder">
<![CDATA[

$(document).ready(function() {
    table = $('#example').DataTable({
        dom: 'Pfrtip',
        colReorder: true
    });
});
]]>
</js>

<title lib="searchPanes">Im the test</title>

<info><![CDATA[

This is the searchPanes extension for DataTables. It allows results to be filtered based on the values of columns.

This example shows that searchPanes is not visible when no records are present in the table as default, and a customised string can be displayed instead.

<button id="populate">populate</button>
<button id="rebuildPane">rebuild Panes</button>

]]></info>

</dt-example>
